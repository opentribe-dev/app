# OpenCrew app

This repository owns the official web and PWA client. Keep provider, runtime, model, and agent concepts distinct. Temporary backend data must be isolated behind `src/lib/gateway.ts` and marked `TODO(CONTRACT)`.
