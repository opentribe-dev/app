# OpenCrew App

The official OpenCrew web/PWA client.

```sh
npm install
npm run dev
npm run build
```

The current v0.1 shell uses a typed local gateway so the complete interaction model can be reviewed while server contracts are finalized. Replace only `src/lib/gateway.ts` when the API and event stream land.
